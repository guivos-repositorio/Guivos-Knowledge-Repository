name: Sync PAS-001 Candidate

on:
  pull_request:
    branches:
      - main
    paths:
      - docs/product-architecture/pas-001-guivos-journey-1.0.0-candidate.md
      - scripts/sync_pas_001_candidate.py
      - .github/workflows/sync-pas-001-candidate.yml

permissions:
  contents: write

jobs:
  synchronize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout candidate branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 1
          show-progress: false

      - name: Preserve canonical PAS-001 checksum
        run: sha256sum docs/product-architecture/pas-001-guivos-journey.md > /tmp/pas-before.sha

      - name: Apply validated mission identifier
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('scripts/sync_pas_001_candidate.py')
          text = path.read_text(encoding='utf-8')
          old = 'Executar a **Validação Editorial e Normativa da Edição Candidata do PAS-001 — Guivos Journey 1.0.0**, preservando o arquivo canônico em `Draft 0.5.0` até decisão formal `Ready for publication`.'
          new = 'Executar `PAS-001-RELEASE-VALIDATION-001` — **Validação Editorial e Normativa da Edição Candidata do PAS-001 — Guivos Journey 1.0.0**, preservando o arquivo canônico em `Draft 0.5.0` até decisão formal `Ready for publication`.'
          if text.count(old) != 1:
              raise SystemExit('mission replacement anchor not found exactly once')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
          PY

      - name: Synchronize canonical artifacts
        run: |
          set +e
          python scripts/sync_pas_001_candidate.py > /tmp/sync.log 2>&1
          status=$?
          cat /tmp/sync.log
          if [ "$status" -ne 0 ]; then
            cp /tmp/sync.log candidate-sync-error.txt
            echo "SYNC_FAILED=1" >> "$GITHUB_ENV"
          else
            echo "SYNC_FAILED=0" >> "$GITHUB_ENV"
          fi

      - name: Publish diagnostic only
        if: env.SYNC_FAILED == '1'
        run: |
          git reset --hard HEAD
          cp /tmp/sync.log candidate-sync-error.txt
          git config user.name "guivos-repositorio"
          git config user.email "guivos-repositorio@users.noreply.github.com"
          git add candidate-sync-error.txt
          git commit -m "Atualiza diagnóstico da sincronização candidata"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

      - name: Stop after diagnostic
        if: env.SYNC_FAILED == '1'
        run: exit 1

      - name: Validate canonical PAS-001 remains unchanged
        if: env.SYNC_FAILED == '0'
        run: |
          sha256sum docs/product-architecture/pas-001-guivos-journey.md > /tmp/pas-after.sha
          diff -u /tmp/pas-before.sha /tmp/pas-after.sha

      - name: Commit synchronized artifacts
        if: env.SYNC_FAILED == '0'
        run: |
          git config user.name "guivos-repositorio"
          git config user.email "guivos-repositorio@users.noreply.github.com"
          git add README.md CHANGELOG.md docs/index.md docs/product-architecture/index.md docs/roadmap.md docs/project/knowledge-board.md docs/project/canonical-consolidation-matrix.md mkdocs.yml
          if git diff --cached --quiet; then
            echo "No synchronization changes required"
          else
            git commit -m "Sincroniza artefatos da edição candidata do PAS-001"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi
