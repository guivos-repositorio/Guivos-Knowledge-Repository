name: Temporary PR350 Sync Propagation

on:
  push:
    branches:
      - agent/gkr-global-audit-lote-d-close-e-next-v1

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/gkr-global-audit-lote-d-close-e-next-v1
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install documentation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt PyYAML
      - name: Apply targeted propagation sync
        run: python .github/tmp/pr350_sync_propagation.py
      - name: Validate repository
        run: |
          python scripts/validate_semantic_state.py
          python scripts/validate_gkr.py
          python scripts/validate_legacy_nomenclature.py
          git diff --check
          mkdocs build --strict --site-dir /tmp/gkr-site
      - name: Commit governed result and remove temporary tooling
        run: |
          rm -f .github/tmp/pr350_sync_propagation.py
          rm -f .github/workflows/temp-pr350-sync-propagation.yml
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          expected=$'.github/tmp/pr350_sync_propagation.py\n.github/workflows/temp-pr350-sync-propagation.yml\ndocs/governance-framework/brand-public-authority-propagation.md'
          test "$(git diff --cached --name-only | sort)" = "$expected"
          git commit -m "GKR: sincronizar propagation com estado e Roadmap vigentes"
          git push origin HEAD:agent/gkr-global-audit-lote-d-close-e-next-v1
